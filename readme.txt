Ввод строк с клавиатуры и образование нового массива
1. Объявление переменных
ссылка на массив строк
дополнительная переменная-ссылка - сохраняет старый массив строк
массив строк длина которых меньше, либо равна 3 символам
дополнительная переменная-ссылка - сохраняет старый массив строк
строки для вывода массивов
текущее количество строк в массиве
текущее количество строк в массиве длина которых меньше, либо равна 3 символам
2. Цикл ввода строк
выделить память для 0 строк
выделить память для 0 строк
Ввести строку
Проверка, пустая ли строка
если строка не пустая, то добавить строку в массив
предварительно выделить память для нового массива в котором на 1 элемент больше
скопировать старый массив в новый
добавить последнюю введенную строку в массив ArrayString2
перенаправить ссылку ArrayString на ArrayString2
Проверка, меньше ли 4 символов в строке
если строка меньше, либо равна 3 символам, то добавить строку в массив
предварительно выделить память для нового массива в котором на 1 элемент больше
скопировать старый массив в новый
добавить последнюю введенную строку в массив ArrayString4
перенаправить ссылку ArrayString3 на ArrayString4
3. Вывод массива строк ArrayString